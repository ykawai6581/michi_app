import { useCallback, useEffect, useMemo, useRef, useState } from 'react'
import { CameraPanel } from './components/CameraPanel'
import { ActiveFeatureOverlay } from './components/ActiveFeatureOverlay'
import { DataPanel } from './components/DataPanel'
import { LayerPanel } from './components/LayerPanel'
import { MapView, type MapHandle } from './components/MapView'
import { SearchPanel } from './components/SearchPanel'
import { StylePanel } from './components/StylePanel'
import { exportPNG } from './export/exportPNG'
import { loadProject, type ProjectData } from './data/project'
import type { EntityFeature, HighlightStyle, LayerVisibility, PointOverlayStyle, RoadSourceVisibility, SceneItem } from './types/geo'
import { initialLayerVisibility, initialPointOverlayStyle } from './map/overlayState'
export default function App(){ const mapRef=useRef<MapHandle>(null);const [project,setProject]=useState<ProjectData|null>(null);const [sceneItems,setSceneItems]=useState<SceneItem[]>([]);const [activeFeature,setActiveFeature]=useState<EntityFeature|null>(null);const [roadSources,setRoadSources]=useState<RoadSourceVisibility>({n13:true,osm:false});const [dataLoading,setDataLoading]=useState(true);const [dataLoadError,setDataLoadError]=useState(false);const [ready,setReady]=useState(false);const [mobileOpen,setMobileOpen]=useState(false);const [style,setStyle]=useState<HighlightStyle>({roadColor:'#ef6262',locationColor:'#64c2f2',regionColor:'#3264aa',width:7,opacity:1,glow:true,animate:true,annotationSize:'large'});const [layers,setLayers]=useState<LayerVisibility>(initialLayerVisibility);const [pointStyle,setPointStyle]=useState<PointOverlayStyle>(initialPointOverlayStyle);useEffect(()=>{let active=true;loadProject().then((value)=>{if(active){setSceneItems([]);setActiveFeature(null);setProject(value)}}).catch(()=>{if(active)setDataLoadError(true)}).finally(()=>{if(active)setDataLoading(false)});return()=>{active=false}},[]);const onReady=useCallback(()=>setReady(true),[]);const toggleFeature=useCallback((feature:EntityFeature)=>setSceneItems((current)=>{const existing=current.find((item)=>item.feature.properties.id===feature.properties.id);const selecting=!existing||!existing.visible;setActiveFeature(selecting?feature:null);return existing?current.map((item)=>item===existing?{...item,visible:selecting}:item):[...current,{feature,visible:true}]}),[]);const deleteFeature=(id:string)=>{setSceneItems((current)=>current.filter((item)=>item.feature.properties.id!==id));setActiveFeature((current)=>current?.properties.id===id?null:current)};const visibleFeatures=useMemo(()=>sceneItems.filter((item)=>item.visible).map((item)=>item.feature),[sceneItems]);return <main className="app"><header><div className="brand-mark">道</div><div className="brand"><strong>MICHI MAP</strong><span>Historical scene editor</span></div><div className="header-context"><span>PROJECT</span><b>{project?.config.displayName ?? '読み込み中…'}</b></div><button className="mobile-toggle" onClick={()=>setMobileOpen(!mobileOpen)}>編集パネル</button><button className="export" disabled={!ready} onClick={()=>{const map=mapRef.current?.getMap();if(map)exportPNG(map)}}><span>↓</span> PNGを書き出す</button></header><div className="workspace"><div className={`map-stage ${layers.basemap==='dark'?'dark-map':''}`}><MapView ref={mapRef} project={project} selected={visibleFeatures} highlightStyle={style} visibility={layers} pointStyle={pointStyle} roadSources={roadSources} onSelectFeature={toggleFeature} onReady={onReady}/><ActiveFeatureOverlay feature={activeFeature}/><div className="legend"><span><i className="modern"/>現代道路</span><span><i className="historic"/>歴史街道</span><span><i className="point"/>宿場・地名</span></div></div><aside className={`sidebar ${mobileOpen?'open':''}`}><SearchPanel entities={project?.searchable??[]} loading={dataLoading} loadError={dataLoadError} items={sceneItems} onToggle={toggleFeature} onDelete={deleteFeature} onClear={()=>{setSceneItems([]);setActiveFeature(null)}}/><LayerPanel value={layers} onChange={setLayers} pointStyle={pointStyle} onPointStyleChange={setPointStyle} roadSources={roadSources} onRoadSourcesChange={setRoadSources}/><StylePanel value={style} onChange={setStyle}/><CameraPanel getMap={()=>mapRef.current?.getMap()??null}/><DataPanel/><footer>地図: {layers.basemap==='rekichizu'?'れきちず / Rekichizu (CC BY-NC-ND 4.0)':'OpenFreeMap / OSM'} · Project bundle: {project?.config.id ?? '…'} <span>v0.3-alpha</span></footer></aside></div></main>}
