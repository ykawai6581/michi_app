import type { LayerVisibility } from '../types/geo'
const labels: Record<keyof LayerVisibility,{name:string;hint:string}> = { base:{name:'白地図',hint:'背景'}, roads:{name:'現代道路',hint:'現在'}, historicalRoads:{name:'江戸街道',hint:'歴史'}, places:{name:'宿場・地名',hint:'地点'}, chome:{name:'町丁目',hint:'境界'} }
export function LayerPanel({ value,onChange }: { value:LayerVisibility; onChange:(v:LayerVisibility)=>void }) { return <section className="panel-section"><div className="section-heading"><span className="eyebrow">LAYERS</span><h2>重ねる情報</h2></div><div className="layer-list">{(Object.keys(labels) as (keyof LayerVisibility)[]).map(key=><label className="layer-row" key={key}><span className={`layer-swatch ${key}`} /><span><strong>{labels[key].name}</strong><small>{labels[key].hint}</small></span><input type="checkbox" checked={value[key]} onChange={()=>onChange({...value,[key]:!value[key]})}/><i aria-hidden="true" /></label>)}</div></section> }
